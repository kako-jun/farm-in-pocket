{
  "name": "@farm-in-pocket/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "node -e \"const fs=require('fs');const t=fs.readFileSync('wrangler.toml','utf8');if(t.includes('REPLACE_WITH_PROD_DATABASE_ID')){console.error('ERROR: wrangler.toml の database_id がプレースホルダーです。wrangler d1 create farm-in-pocket で本番 DB を作成し、ID を貼り替えてください。');process.exit(1)}\" && wrangler d1 migrations apply DB --remote",
    "db:reset:local": "rm -rf .wrangler/state/v3/d1 && wrangler d1 migrations apply DB --local"
  },
  "dependencies": {
    "@farm-in-pocket/shared": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/better-sqlite3": "^7.6.0",
    "@vitest/coverage-v8": "^3.0.0",
    "better-sqlite3": "^11.3.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
