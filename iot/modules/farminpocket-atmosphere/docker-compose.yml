version: '3.8'

services:
  farminpocket-atmosphere:
    build: .
    container_name: farminpocket-atmosphere
    volumes:
      - ./data:/data
      - ./config.json:/app/config.json
    restart: unless-stopped
    labels:
      - "version=1.0.0"
      - "description=温度・湿度監視"
    # 実際のセンサー使用時はprivilegedモードが必要
    # privileged: true
    # devices:
    #   - /dev/gpiomem:/dev/gpiomem
